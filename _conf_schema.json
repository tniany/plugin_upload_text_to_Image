{
  "image_url": {
    "description": "图片模板地址",
    "type": "string",
    "default": "https://api.suyanw.cn/api/comic.php",
    "hint": "用于生成图片的模板地址，默认使用二次元风格模板"
  },
  "color_enabled": {
    "description": "开启/关闭 文本多颜色",
    "type": "bool",
    "default": true,
    "hint": "开启后生成的图片将包含多种颜色，关闭后生成的图片将只有一种颜色"
  },
  "max_text_length": {
    "description": "文本字数上限",
    "type": "int",
    "default": 1000,
    "hint": "生成图片时的文本字数上限，超过此限制将拒绝生成图片"
  },
  "max_url_length": {
    "description": "URL长度上限",
    "type": "int",
    "default": 2048,
    "hint": "生成的图片URL长度上限，超过此限制将不转换为图片"
  }
}